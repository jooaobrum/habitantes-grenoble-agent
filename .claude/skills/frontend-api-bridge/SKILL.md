---
name: frontend-api-bridge
version: "1.0"
last_reviewed: "2025-01"
description: Define minimal UI input model and API contract (POST /ask, POST /feedback) and plan simplest UI integration.
---

# Frontend ↔ API Bridge Skill

## Deliverables
1) Inputs: fields, validation, defaults
2) API request/response JSON examples for:
   - POST /ask
   - POST /feedback
3) UI states: idle → loading → result | empty | error
4) 1-screen flow preferred
